# ★2026-08-18 追加：上げるつもりのないものを、うっかり公開しないための決まり
# （この日、git add の指定が広すぎて 作業用モックと編集前の控えを数分だけ公開してしまった）

# 編集前の控え
_backup/

# 作業用のモック（承認済みのものは mocks/ に置く）
mock_general_hold_reserve.html
mock_hq_matrix.html
mock_pickup_by_client.html

# GASのコード（パスワードとLINEのIDが入っているので、絶対に公開しない）
tests/gas/
*コード.js
.clasp.json